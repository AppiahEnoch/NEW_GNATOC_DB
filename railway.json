{
    "$schema": "https://gnatock.com/railway.schema.json",
    "build": {
      "builder": "NIXPACKS",
      "buildCommand": "sudo mkdir -p output && sudo chmod -R 0777 output/;sudo mkdir -p file && sudo chmod -R 0777 file/"
    },
    "deploy": {
      "restartPolicyType": "ON_FAILURE",
      "restartPolicyMaxRetries": 10
    }
  }